import styled from 'styled-components';

const PageTitleHeader = styled.div`
	overflow: auto;
	padding-bottom: 0px;
	margin-bottom: 10px;
	border-bottom: black 3px solid;
`

const PageTitleText = styled.div`
	float: left;
`

const UserButtonArea = styled.div`
	float:right;
	margin:20px 30px 0px 0;
`

export { PageTitleHeader, PageTitleText, UserButtonArea }