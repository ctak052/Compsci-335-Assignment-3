import styled from "styled-components";

const CommentForm = styled.form`
  width: 100%;
  padding: 15px;
`;

const CommentText = styled.textarea`
  width: 90%;
`;

const UsersName = styled.input`
  width: 300px;
`;

export {CommentForm, CommentText, UsersName}